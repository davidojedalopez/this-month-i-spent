{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "buildFunctions": "cd netlify/functions/ && npm install && cd ../../",
    "build": "npm run buildFunctions && nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@types/node": "^18",
    "nuxt": "^3.5.2"
  },
  "dependencies": {

  }
}
